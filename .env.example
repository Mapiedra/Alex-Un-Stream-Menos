# Telemetria anonima de partidas. OPCIONAL: sin estas variables el juego
# funciona exactamente igual, simplemente no manda nada.
#
# El esquema esta en docs/supabase.sql. La clave anonima acaba en el bundle
# —la ve cualquiera— y por eso la politica RLS solo le permite INSERTAR.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
